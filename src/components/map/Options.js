import React, { Component } from 'react'

export class Options extends Component {
    render() {
        return (
            <div>
                options
            </div>
        )
    }
}

export default Options
